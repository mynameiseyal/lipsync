# Core AI/ML frameworks with CUDA support
# Install PyTorch with CUDA support using: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=1.8.0
torchvision>=0.9.0
torchaudio>=0.8.0

# Alternative: CPU versions (comment the above and uncomment below for CPU-only)
# torch>=1.8.0
# torchvision>=0.9.0
# torchaudio>=0.8.0

# Computer Vision and Image Processing
opencv-python>=4.6.0
opencv-contrib-python>=4.6.0
Pillow>=8.3.0
# face-recognition>=1.3.0  # REMOVED - not used in codebase, requires dlib/CMake
# retinaface>=0.0.12  # Commented out - causes TensorFlow 2.5.0 conflict
mediapipe>=0.10.0

# GPU-accelerated image processing (optional - may not work on Python 3.12)
# cupy-cuda11x>=10.0.0  # GPU-accelerated NumPy
# cucim>=22.02.00       # GPU-accelerated image processing

# Background Removal and Image Processing
rembg>=2.0.50
backgroundremover>=0.2.2
# segmentation-models-pytorch>=0.3.0  # May conflict with other packages

# Video Processing and Editing
moviepy>=1.0.3
ffmpeg-python>=0.2.0
imageio>=2.9.0
imageio-ffmpeg>=0.4.4

# Audio Processing and TTS
coqui-tts>=0.13.0     # Fixed package name (was TTS>=0.13.0)
gTTS>=2.2.3           # Fallback TTS
librosa>=0.8.1
soundfile>=0.10.3
pydub>=0.25.1
# espnet>=202301        # May cause conflicts - commented out
# speechbrain>=0.5.12   # May cause conflicts - commented out

# Scientific Computing
numpy>=1.21.0
scipy>=1.7.0
scikit-image>=0.18.0

# GPU Memory and Performance
pynvml>=11.4.1        # NVIDIA GPU monitoring
psutil>=5.8.0         # System monitoring
accelerate>=0.12.0    # Hugging Face accelerate for GPU optimization

# Utilities and Configuration
requests>=2.25.1
tqdm>=4.62.0
colorama>=0.4.4
click>=8.0.0

# Optional: UI frameworks
gradio>=3.0.0
streamlit>=1.15.0

# Development tools
jupyter>=1.0.0
matplotlib>=3.3.0

# Additional utilities (compatible alternatives)
transformers>=4.15.0  # For modern ML models
# diffusers>=0.10.0     # For diffusion models if needed - may cause conflicts
# xformers>=0.0.16      # Memory-efficient transformers - may cause conflicts 